/**
 * Login button
 */

"use client"
import { devLog } from "@take/lib/logger";

export default function LoginButton() {


    return (
        <button
            type="button"
            
        >
            Login to access
        </button>
    )
}